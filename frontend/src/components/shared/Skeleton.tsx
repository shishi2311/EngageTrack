/** Placeholder — full implementation in Phase 2 */
export default function Skeleton({ width = '100%', height = '16px' }: { width?: string; height?: string }) {
  return <div className="skeleton" style={{ width, height }} />
}
