import PageHeader from '../components/layout/PageHeader'

export default function Dashboard() {
  return (
    <div className="page-enter">
      <PageHeader title="Dashboard" subtitle="Overview of all client engagements" />
      <p style={{ color: 'var(--text-secondary)' }}>Dashboard — coming in Phase 2.</p>
    </div>
  )
}
